# 前端环境变量示例（复制为 .env）

# API 配置（可选）
# 默认走同域相对路径 /api：
#   - 生产（同域反代）：前端容器 nginx 将 /api、/images 反代到后端，无需设置本项；
#     一个前端构建产物即可跨环境复用。
#   - 开发：由 vite server.proxy 将 /api、/images 代理到本地后端（http://localhost:8080）。
# 仅前后端分域名部署时才设置本项，值需包含 /api 后缀，例如：
#   VITE_API_BASE_URL=https://api.yourdomain.com/api

# VITE_API_BASE_URL=

# 用户相关配置（如注册是否强制邮箱、是否允许修改用户名）请在后端 .env 文件中配置，前端会自动从 /api/config 接口获取
